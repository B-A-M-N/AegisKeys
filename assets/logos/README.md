# Logo Assets

Place approved logo PNG assets here for matrix-rain silhouettes.

The TUI does not hand-invent ASCII logos. It loads each registered logo asset,
samples it into a terminal-cell mask, and uses that mask to modulate matrix rain
brightness, density, stability, and color.

Expected filenames are declared in `internal/logo/assets.go`.
